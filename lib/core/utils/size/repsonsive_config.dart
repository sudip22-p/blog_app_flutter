class ResponsiveConfig {
  // Scale factor limits
  static const double maxScaleFactor = 2.2;
  static const double minScaleFactor = 0.85;

  // Font scaling limits (more conservative for readability)
  static const double maxFontScaleFactor = 1.6;
  static const double minFontScaleFactor = 0.9;

  // Breakpoints
  static const double tabletBreakpoint = 600;
  static const double desktopBreakpoint = 1200;
}
